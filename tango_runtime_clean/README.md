# Tango Runtime (AI-Free Clean Scaffold)

This folder contains a clean runtime for orchestrating Tango instances without any AI.
See `run.py` for the entry point and `controllers/random_controller.py` for a simple controller.
